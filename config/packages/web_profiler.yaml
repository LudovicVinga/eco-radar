when@dev:
    web_profiler:
        toolbar: true

    framework:
        profiler:
            enabled: true

when@test:
    web_profiler:
        toolbar: false
        intercept_redirects: false

    framework:
        profiler:
            collect: false
