<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}EcoRadar{% endblock %}</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter&family=Poppins:wght@600&display=swap" rel="stylesheet">

    <!-- Custom Styles (si besoin) -->
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">

    {% block stylesheets %}{% endblock %}
</head>
<body style="font-family: 'Inter', sans-serif; background-color: #F5F5F5; color: #333;">
    <header class="bg-dark text-white py-3">
        <div class="container d-flex justify-content-between align-items-center">
            <img src="{{ asset('images/logo-ecoradar.png') }}" alt="EcoRadar" style="height: 50px;">
            <nav>
                <a href="{{ path('explorer') }}" class="text-white me-3 text-decoration-none">Explorer</a>
                <a href="{{ path('visualisations') }}" class="text-white me-3 text-decoration-none">Visualisations</a>
                <a href="{{ path('a_propos') }}" class="text-white text-decoration-none">À propos</a>
            </nav>
        </div>
    </header>

    <main class="container py-5">
        {% block body %}{% endblock %}
    </main>

    <footer class="bg-dark text-white text-center py-4 mt-auto">
        <p>© 2025 EcoRadar — Tous droits réservés</p>
    </footer>

    <!-- Bootstrap JS (avec Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
</body>
</html>
